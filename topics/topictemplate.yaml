apiVersion: kafka.strimzi.io/v1alpha1
kind: KafkaTopic
metadata:
  name: {{ TOPIC_NAME }}
  namespace: {{ NAMESPACE }}
  labels:
    strimzi.io/cluster: {{ KAFKA_CLUSTER }}
spec:
  config: {}
  partitions: {{ PARTITIONS }}
  replicas: {{ REPLICAS }}
